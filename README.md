# ESG Vault

A simple web application for Indian companies to calculate ESG (Environmental, Social, and Governance) metrics.

## How to run
1. Install dependencies: `pip install -r requirements.txt`
2. Run the app: `streamlit run app.py`
